package auth

//todo: this file is not needed; maybe you should rename the sessions and users files to sessions-db and users-db respectively

//low level drivers

// todo: the following two functions should be grouped into one, which returns struct credentials {...}, which in turn should be in struct userInfo{...}
